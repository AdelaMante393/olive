# OpenResourceHub

OpenResourceHub 是一个面向技术内容创作者与本地化团队的媒体资源导航与元数据聚合平台。项目定位为高质量中文视频资源的外部索引与结构化信息枢纽，不存储、不托管、不分发任何实体媒体文件，仅对公开可访问的第三方资源站点进行信息分类、可用性监测与访问导航。目标用户包括独立字幕组、多语言内容研究者、海外华语媒体观察者以及需要批量检索中文视频公开资源的自动化脚本开发者。通过本项目的资源分类体系与周期性可用性检测脚本，用户能够显著降低人工筛选无效或低质量资源站点的成本，将精力集中于内容本身的质量评估与二次创作。

## 功能概览

- **资源站点分级索引** 按照内容语言、字幕完备性、视频清晰度与更新频率对收录站点进行标签化分类，支持多维度筛选与排序。

- **可用性自动化检测** 内置基于 HTTP 状态码与响应时长的周期性检测模块，可生成各资源站点的服务可用率报表，辅助用户识别稳定来源。

- **元数据提取与结构化** 对目标站点的公开页面标题、描述、关键词进行非侵入式抓取与规范化存储，形成可机器读取的 JSON 元数据集。

- **访问路由优化建议** 基于地理分布与 DNS 解析时延，为不同区域的用户推荐最优访问入口，减少跨洲访问丢包率。

- **变更追踪与通知** 监控收录站点的页面结构变更与域名解析变动，通过日志文件记录差异，便于用户及时调整本地采集脚本。

- **开源规则库维护** 将资源筛选过程中的经验沉淀为可配置的过滤规则集，用户可自行启用或禁用特定规则以适应不同地区的网络访问策略。

- **批量导出与集成支持** 提供 CSV 与 JSON 格式的完整资源列表导出接口，便于第三方工具（如自动化下载器、内容分析系统）无缝集成。

## 应用场景

**华语视频内容研究** 高校传媒专业的研究人员利用本项目的分类索引快速定位特定时期、特定类型的中文视频公开资源，用于内容趋势分析与话语特征研究，大幅缩短样本收集周期。

**字幕组协作与资源发现** 多语言字幕组在翻译新剧或纪录片时，通过本项目的可用性检测报表筛选当前响应最快的字幕站点，优先获取高质量中文字幕对照素材，提升字幕制作效率。

**海外个人开发者内容聚合** 海外独立开发者基于本项目的导出接口，快速构建轻量级中文视频搜索工具，无需自行维护庞大的站点列表与可用性检测逻辑，降低项目维护成本。

**网络资源归档辅助** 数字存档志愿者利用本项目的变更追踪功能，及时发现资源站点的迁移或下线，调整归档任务队列，避免因链接失效导致的存档空白。

## 快速开始

以下步骤将指导您在本地环境中完整部署 OpenResourceHub 的核心检测与索引模块。

```bash
# 1. 克隆项目仓库至本地
git clone https://github.com/openresourcehub/openhub-core.git
cd openhub-core

# 2. 安装项目依赖（使用 pip 进行包管理，Python 3.9+ 必需）
pip install -r requirements.txt

# 3. 执行首次资源站点可用性全量检测，并生成报表文件至 output/ 目录
python cli.py --scan --full --export-json
```

## 安装要求

运行本项目前，请确保目标环境满足以下依赖条件。若检测脚本依赖特定系统库，请参照说明提前安装。

| 依赖项 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 及以上 | 核心运行环境，所有脚本基于 Python 开发，低版本将导致语法错误 |
| pip | 21.0 及以上 | Python 包管理工具，用于安装 requests、lxml、pandas 等第三方库 |
| Git | 2.25 及以上 | 用于克隆仓库及后续拉取更新，不支持旧版 Git 协议 |
| 网络连接 | 可访问公网 | 检测模块需向外发起 HTTP 请求，内网环境需配置代理或镜像源 |
| 系统时区 | UTC+8 建议 | 日志时间戳默认使用系统本地时间，非 UTC+8 可能影响报表日期对齐 |
| 磁盘空间 | 500 MB 以上 | 用于存储历史检测记录与元数据缓存，长期运行建议预留 2 GB 以上 |
| 内存 | 1 GB 以上 | 加载完整资源列表（约 2000+ 条目）进行并发检测时，内存占用峰值约 800 MB |

## 文档导航

为帮助用户快速定位所需信息，项目文档按受众与使用阶段划分为以下四个层面，建议新用户从“入门指南”开始阅读。

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 入门指南 | docs/getting-started/ | 如何安装、配置首次扫描、理解输出报表的基本字段含义 |
| 运维手册 | docs/operations/ | 如何调整检测频率、配置告警阈值、备份历史数据 |
| 开发参考 | docs/development/ | 如何扩展新的资源解析器、自定义过滤规则、提交补丁 |
| API 接口 | docs/api/ | 导出接口的请求格式、返回字段说明、分页与排序参数 |

## 资源列表

本项目索引的外部资源站点均为公开可访问的中文视频与字幕相关导航页，以下列表按收录批次与主题进行分组。所有 URL 均以原始形式呈现，未做任何协议或域名改写。

### 第 29/63 批次收录站点

<code>guochanzhubozaixianguankan.org.cn</code>

<code>zhongwenshipin.org.cn</code>

<code>zaixianbofangzhongwenzimu2.org.cn</code>

<code>zhongwenzimugaoqing.org.cn</code>

<code>renqimianfeishipin.org.cn</code>

<code>zhongwenzimuzaixiankan.org.cn</code>

<code>zuixinzhongwenzimu.org.cn</code>

## 项目结构

项目采用模块化分层设计，核心检测引擎与数据模型相互隔离，便于单元测试与功能扩展。以下为 src/ 目录下的主要子模块说明。

```
openhub-core/
│
├── src/
│   ├── scanner/                 # 核心检测引擎，负责发送 HTTP 请求与解析响应
│   │   ├── __init__.py
│   │   ├── http_client.py      # 封装 aiohttp 会话，管理连接池与超时重试
│   │   └── status_checker.py   # 实现单站点与批量可用性检测逻辑
│   │
│   ├── parser/                  # 页面元数据解析器，针对不同站点结构提取信息
│   │   ├── __init__.py
│   │   ├── html_extractor.py   # 基于 XPath 与正则的通用字段抽取
│   │   └── site_rules.yaml     # 各站点特有的解析规则配置文件
│   │
│   ├── storage/                 # 数据持久化层，支持 JSON 与 CSV 两种输出格式
│   │   ├── __init__.py
│   │   ├── exporter.py         # 导出接口，支持分页、排序与字段过滤
│   │   └── cache_manager.py    # 本地缓存管理，减少重复网络请求
│   │
│   ├── notifier/                # 变更通知与日志记录模块
│   │   ├── __init__.py
│   │   ├── diff_reporter.py    # 比较两次扫描结果，生成差异摘要
│   │   └── file_logger.py      # 按日期轮转的日志写入器
│   │
│   └── cli/                     # 命令行入口，解析用户参数并调度各模块
│       ├── __init__.py
│       └── main.py             # 注册 scan、export、validate 等子命令
│
├── tests/                       # 单元测试与集成测试用例，覆盖核心检测路径
│   ├── test_http_client.py
│   ├── test_parser.py
│   └── test_exporter.py
│
├── config/                      # 全局配置文件，包含超时、并发数、重试策略
│   └── settings.toml
│
├── output/                      # 运行时生成的报表与缓存文件目录（gitignore）
│   ├── reports/
│   └── cache/
│
├── docs/                        # 完整项目文档，包含入门、运维、开发与 API
│   ├── getting-started/
│   ├── operations/
│   ├── development/
│   └── api/
│
├── requirements.txt             # Python 依赖清单，锁定主要版本号
├── setup.py                     # 项目安装脚本，支持 pip install -e .
└── README.md                    # 本文件
```

## 贡献指南

开源项目的持续发展离不开社区的反馈与贡献。我们欢迎各类形式的参与，包括但不限于新增资源站点规则、优化检测算法、改进文档与修复缺陷。请按照以下步骤提交您的贡献。

1. **查阅问题追踪器与讨论区** 访问 GitHub Issues 页面，确认您要处理的问题或建议未被其他人认领。对于较大的功能变更，建议先通过 Discussion 发起设计讨论，避免无效工作。

2. **派生仓库并创建功能分支** 将主仓库派生至您个人的 GitHub 账号下，然后基于最新的 main 分支创建以 feature/ 或 fix/ 为前缀的分支名，例如 feature/add-rtmp-checker。

3. **编写代码与单元测试** 所有新增功能或缺陷修复均需附带对应的单元测试用例，确保测试覆盖率达到 85% 以上。运行 pytest 验证本地所有用例通过后再提交。

4. **遵循代码风格规范** 项目使用 Black 与 isort 进行自动格式化，提交前请执行 make format 命令统一代码风格。提交信息请遵循 Conventional Commits 规范，使用 feat:、fix:、docs: 等类型前缀。

5. **发起拉取请求并完成审查** 将您的分支推送至派生仓库，然后向主仓库的 main 分支发起 Pull Request。在 PR 描述中清晰说明变更内容、测试结果与影响范围。至少两名维护者审查通过后即可合并。

## 常见问题

**问：检测脚本报告大量站点超时，是否表示这些资源已完全不可用？**

答：不一定。超时可能由临时网络波动、目标站点的反爬机制（如限速）或地理路由变化引起。项目默认超时设置为 15 秒，并包含三次重试机制。若连续三次全量扫描（间隔 24 小时）均报告超时，则该站点会被标记为“待观察”。建议用户结合自身网络环境调整 config/settings.toml 中的 timeout 与 retry 参数。同时，可尝试使用不同的网络出口（如切换 VPN 节点）进行交叉验证。

**问：如何更新已收录站点的解析规则，以适应目标站点改版？**

答：当目标站点更新页面结构导致元数据提取失败时，您无需修改核心代码。请直接编辑 src/parser/site_rules.yaml 文件，在该站点对应的条目下更新 xpath_title 或 regex_keywords 字段。修改后运行 python cli.py --validate-rules 验证新规则的有效性。若规则生效，建议按照贡献指南提交 Pull Request，以便其他用户受益。

**问：本项目是否提供 Docker 镜像或一键部署方案？**

答：目前项目官方未提供预构建的 Docker 镜像，但社区贡献者维护了一份 Dockerfile 位于 contrib/docker/ 目录下。您可以使用 docker build -t openhub-core . 自行构建镜像，并挂载本地 output/ 目录以持久化报表。若您希望使用一键部署脚本，可参考 contrib/deploy.sh，该脚本会自动检测系统环境并安装所需依赖。需要注意，该脚本目前仅支持 Debian/Ubuntu 系发行版，CentOS 用户需手动调整包管理器命令。

## 许可证

本项目的源代码与文档采用 MIT 许可证进行分发。您可以在遵守许可证条款的前提下自由使用、修改、复制、合并、发布、再许可及销售本软件的副本。完整的许可证文本请参见项目根目录下的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-25 21:59:38
