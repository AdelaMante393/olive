# Nexus Archive

Nexus Archive 是一个面向开发人员与内容研究者的技术资源聚合与导航系统。该项目定位于构建高可用、可扩展的外链资源管理平台，通过结构化元数据与自动化校验机制，解决分散资源难以统一检索、状态监控与版本追溯的痛点。目标用户包括运维工程师、数据采集开发者、SEO 研究人员以及需要长期维护大量外链引用的项目团队。

## 功能概览

- **多源资源聚合**：支持批量导入外部链接，自动识别协议类型与域名层级，生成标准化的资源条目。
- **实时可用性检测**：内置异步健康检查模块，可定时探测各资源端点的响应状态与 HTTP 状态码。
- **元数据标注系统**：允许用户为每条资源添加分类标签、失效日期、地理区域与内容摘要，便于精细化管理。
- **快速检索与过滤**：提供基于域名、关键字、状态、标签组合的实时搜索接口，响应时间低于 200 毫秒。
- **变更历史审计**：记录每次资源新增、删除、字段修改的完整操作日志，支持按时间线回溯。
- **批量导入导出**：支持 CSV 与 JSON 格式的批量数据迁移，兼容常见爬虫框架的输出结构。
- **只读镜像模式**：允许生成当前资源列表的静态只读副本，用于前端展示或第三方系统集成。

## 应用场景

- **运维监控面板辅助**：运维团队可将本系统作为外链依赖的次级监控层，定期核对上游资源可达性，在 CI/CD 流水线中嵌入健康检查钩子。
- **内容聚合站点的资源库**：面向影视、文档、资讯类内容站点，用于集中管理外部播放源、字幕源或备用域名，降低人工维护成本。
- **学术研究与数据采集**：研究人员可利用系统的标签与过滤功能，快速筛选特定领域的资源样本，并导出结构化数据集用于统计分析。
- **内部知识库外部链接治理**：企业知识库管理员可使用审计功能追踪文档内外部链接的变更，防止引用失效导致的文档可用性下降。

## 快速开始

以下命令演示了从代码仓库克隆、安装依赖到启动开发服务的完整流程。

```bash
# 克隆项目仓库
git clone https://github.com/nexus-archive/nexus-archive.git

# 进入项目目录
cd nexus-archive

# 安装依赖（使用 npm）
npm install

# 复制环境变量示例文件
cp .env.example .env

# 启动开发服务器（默认端口 3000）
npm run dev
```

启动成功后，访问控制台输出的本地地址即可查看资源管理界面。生产环境部署请参考后续文档章节。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | >= 18.12.0 | 运行时环境，需支持 ES2022 特性 |
| npm | >= 8.19.0 | 包管理器，用于依赖安装与脚本执行 |
| PostgreSQL | >= 14.0 | 主数据库，用于存储资源元数据与审计日志 |
| Redis | >= 6.2 | 缓存与会话存储，用于提升检索性能及分布式锁 |
| Nginx | >= 1.22 | 生产环境反向代理，用于负载均衡与静态资源缓存 |
| Git | >= 2.30 | 版本控制工具，用于克隆与提交代码 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | /docs/user/getting-started.md | 如何注册、登录、添加第一条资源以及进行基础检索 |
| 运维指南 | /docs/ops/deployment.md | 如何配置环境变量、数据库迁移、SSL 证书与容器化部署 |
| 开发者文档 | /docs/dev/api-reference.md | 各 RESTful 接口的请求参数、响应结构与错误码定义 |
| 架构设计 | /docs/arch/data-model.md | 核心数据表关系、索引策略与缓存失效机制 |

## 资源列表

以下为系统内置推荐资源及外部参考链接，按照内容类别组织。所有 URL 严格遵循原始输入格式输出。

### 在线播放与视频源

- <code>zaixianbofang2.org.cn</code>
- <code>zaixianguankanwangyeshipin2.org.cn</code>
- <code>zaixianzhongwenzimu.org.cn</code>

### 字幕资源与辅助素材

- <code>zhubofuli.org.cn</code>
- <code>zhongwenzimudianying.org.cn</code>
- <code>zhongwenzimuwangzhan.org.cn</code>
- <code>zhongwenzimuzaixianmianfeikan2.org.cn</code>

## 项目结构

```
nexus-archive/
├── src/                           # 核心源代码目录
│   ├── controllers/               # 请求控制器，处理路由与业务编排
│   ├── services/                  # 业务逻辑服务层，含资源校验、健康检查
│   ├── models/                    # 数据模型定义（Sequelize / TypeORM）
│   ├── middlewares/               # 认证、日志、限流、错误处理中间件
│   ├── utils/                     # 通用工具函数（URL 解析、日期格式化、加密）
│   └── workers/                   # 后台任务队列（可用性检测、邮件通知）
├── config/                        # 环境配置文件与常量定义
│   ├── database.js                # 数据库连接配置
│   ├── redis.js                   # Redis 连接与缓存策略
│   └── constants.js               # 状态码、标签白名单等全局常量
├── migrations/                    # 数据库迁移脚本（按时间戳命名）
├── seeders/                       # 初始测试数据填充脚本
├── tests/                         # 单元测试与集成测试用例
│   ├── unit/                      # 服务层与工具函数的单元测试
│   └── integration/               # API 接口与数据库交互的集成测试
├── docs/                          # 项目文档（用户手册、运维、API、架构）
├── scripts/                       # 辅助运维脚本（备份、数据清洗、批量导入）
├── public/                        # 静态资源文件（前端打包产物、favicon）
├── logs/                          # 应用日志输出目录（按天轮转）
├── .env.example                   # 环境变量模板
├── package.json                   # npm 项目清单与依赖声明
├── ecosystem.config.js            # PM2 进程管理配置（生产环境）
└── README.md                      # 项目说明文档（本文件）
```

## 贡献指南

我们欢迎社区提交问题报告、功能建议与代码贡献。请遵循以下流程以保证协作效率。

1. 在 GitHub Issues 中查找是否存在相关议题，若无则新建一个议题并详细描述问题或需求，等待维护者回复确认。
2. 从主仓库派生（Fork）项目至个人账户，并基于 `develop` 分支创建以 `feature/` 或 `fix/` 为前缀的功能分支。
3. 完成代码修改后，请确保通过全部单元测试（`npm run test`），并补充必要的测试用例以覆盖新增逻辑。
4. 提交代码时遵循 Conventional Commits 规范（例如 `feat: 添加批量导入进度条` 或 `fix: 修复 URL 编码导致的校验异常`）。
5. 发起 Pull Request 至 `develop` 分支，并在描述中关联对应议题编号。等待至少一位维护者进行代码评审（Code Review）后合并。

## 常见问题

**问：健康检查模块是否会因目标站点防爬策略而频繁触发封锁？**

答：系统默认使用 `axios` 库并携带常见浏览器 UA 头部，且检查间隔默认为 6 小时（可配置）。若目标站点存在严格限流，建议通过环境变量调整检查频率，或在资源条目中手动标记为「谨慎检测」以跳过自动探测。

**问：如何从旧版本数据迁移至当前版本？**

答：项目根目录下提供了 `scripts/migrate-legacy.js` 脚本，支持读取旧版 JSON 导出文件并转换为新版数据模型。请先执行 `npm run migrate:legacy -- --input ./old-data.json --output ./migrated.json` 预览转换结果，确认无误后再通过管理界面导入。

**问：系统是否支持完全离线部署，不依赖任何外部 CDN？**

答：支持。前端所有依赖已打包至 `public` 目录，且后端不调用任何外部 API（除非启用可选的「外部增强检测」功能）。您只需将 `NODE_ENV` 设置为 `production` 并关闭 `ENABLE_EXTERNAL_CHECK` 环境变量即可。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-25 22:00:19
