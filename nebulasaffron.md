# 项目名：MediaNav 开源导航站

MediaNav 是一个面向数字媒体从业者、内容创作者及海外华语用户群体的轻量级开源导航与资源聚合项目。其核心定位并非提供内容托管或分发服务，而是作为技术型索引中间层，对互联网公开的华语视频媒体、字幕资源、在线播放工具等垂直领域站点进行逻辑分类与结构化呈现。

项目目标用户包括需要快速检索特定语种媒体资源的开发者、自动化脚本编写者、数据标注人员以及学术研究机构。MediaNav 通过静态站点生成技术（SSG）将外部链接整合为可离线运行的导航页面，有效降低用户在多来源、多域名间的信息检索成本，同时为二次开发与自部署提供完整的技术底座。

本项目不存储、不缓存、不转发任何第三方媒体文件，仅作为 URL 管理框架对外发布。所有指向的外部资源均由其原始提供方负责内容合规性与服务可用性。MediaNav 遵循开源协作理念，鼓励社区提交新增节点、失效链接修正及分类优化建议。

## 功能概览

- **多维度分类索引**：按语种、字幕类型、清晰度、热度等标签对媒体资源站点进行交叉分类，支持快速筛选与排序。

- **自定义导航页生成**：基于 JSON 配置文件动态生成响应式 HTML 页面，用户可自由增删改查导航条目，无需修改核心代码。

- **链接可用性监测**：内置基于 Headless 浏览器的轻量级巡检模块，可定时检测各导航 URL 的 HTTP 状态码与页面加载成功率。

- **离线部署支持**：构建输出为纯静态文件，支持直接挂载至 Nginx、Apache、S3 或 Cloudflare Pages 等任何静态托管环境。

- **多语言界面切换**：导航界面本身提供简体中文、繁体中文及英文三种语言包，便于不同地区的用户操作。

- **标签与全文检索**：支持对导航条目的标题、描述、标签及分类路径进行实时模糊搜索，搜索结果高亮显示。

- **导入导出机制**：导航数据可导出为标准 CSV 或 JSON 格式，亦支持从外部 RSS 源或 OPML 文件批量导入链接。

- **访问统计看板**：集成可选的自部署统计后端，记录各链接的点击频次与来源地域分布，辅助用户评估资源热度。

## 应用场景

- **华语媒体研究资料整理**：高校新闻传播学院或社会学研究机构可利用 MediaNav 汇集特定语种的在线视频与字幕站点，作为舆情分析或内容趋势研究的入口索引，同时通过自定义标签记录每站点的政治倾向、更新频率与受众画像。

- **个人媒体消费导航**：海外华语用户可通过自部署的 MediaNav 页面，将所有常用中文影视、综艺、新闻站点集中于一处，避免频繁切换浏览器书签或记忆复杂域名，并利用可用性监测功能自动屏蔽长期失活的站点。

- **自动化脚本测试靶场**：爬虫开发者或 AI 数据采集工程师可将 MediaNav 作为初始 URL 种子库，利用其导出的 JSON 格式链接列表构建定向爬取任务，配合巡检模块验证目标站点的反爬策略变化。

- **企业内网媒体门户**：跨国企业的中国区或亚太分部可基于 MediaNav 二次开发，搭建内部合规的中文视频培训材料导航系统，仅允许内网访问，并可自由增删企业自建的学习资源链接。

- **公益图书馆数字资源整合**：社区公益图书馆或非营利组织可为视障或老年群体定制大字体、高对比度的导航界面，聚合免费公开的中文有声读物与字幕视频来源，降低数字鸿沟。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，需提前安装 Git 与 Node.js（v18 及以上）。

```bash
# 1. 克隆仓库
git clone https://github.com/medianav/medianav-starter.git
cd medianav-starter

# 2. 安装项目依赖
npm install

# 3. 构建并启动开发服务器
npm run build
npm run serve
```

执行完成后，访问控制台输出的本地地址（默认 http://localhost:8080 ）即可看到导航首页。若需自定义导航数据，请编辑 `./data/navigation.json` 文件后重新运行 `npm run build`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | >= 18.0.0 | 核心运行时环境，用于执行构建脚本与开发服务器 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖及运行脚本命令 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库及后续拉取更新 |
| 现代浏览器 | 最新两个主要版本 | 用于访问生成的导航界面，推荐 Chrome / Firefox / Edge |
| 网络连接 | 稳定互联网访问 | 首次构建需下载 npm 包；导航巡检模块需访问目标 URL |
| 磁盘空间 | >= 200 MB | 存放源码、依赖包及构建产物 |
| 操作系统 | Linux / macOS / Windows 10+ | 支持以上系统的 WSL 或原生环境 |
| Python（可选） | >= 3.8 | 若启用高级巡检脚本（基于 Playwright 的 Python 封装）则需安装 |
| Docker（可选） | >= 20.10 | 若使用容器化部署方式则需安装 Docker Engine |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户指南 | `docs/user-guide/` | 如何部署导航页、如何导入导出书签、如何切换语言及使用搜索功能 |
| 开发者文档 | `docs/developer/` | 导航数据 JSON Schema 定义、API 接口规范、自定义主题开发流程 |
| 运维手册 | `docs/ops/` | 巡检模块配置、日志收集方案、静态托管平台适配指南（Nginx / S3 / Pages） |
| 贡献规范 | `CONTRIBUTING.md` | 提交新导航节点的审核标准、PR 标题格式、Commit 信息规范及测试要求 |
| 设计资产 | `assets/design/` | 界面原型 Figma 文件、Logo 源文件、色彩系统与排版规范说明 |
| 变更日志 | `CHANGELOG.md` | 按语义化版本记录每个 Release 的新增功能、修复缺陷与破坏性变更 |

## 资源列表

以下为 MediaNav 当前版本收录的全部外部导航资源。所有 URL 均按原始输入一字不差列出，未做任何协议补全或域名改写。请用户注意，部分链接可能因外部服务变更而失效，建议配合本项目的巡检模块定期验证。

媒体分类 - 综合视频观看类：
- <code>guochanzhubozaixianguankanw.org.cn</code>
- <code>zhongwenshipinw.org.cn</code>
- <code>zaixianbofangzhongwenzimuw1.org.cn</code>
- <code>zhongwenzimugaoqingw.org.cn</code>
- <code>renqimianfeishipinw.org.cn</code>
- <code>zhongwenzimuzaixiankanw.org.cn</code>
- <code>zuixinzhongwenzimuw.org.cn</code>

## 项目结构

```
medianav-starter/
├── .github/                         # GitHub 社区配置文件
│   ├── ISSUE_TEMPLATE/              # 问题报告与功能请求模板
│   └── workflows/                   # CI 持续集成工作流 (构建与巡检)
├── assets/                          # 静态资源目录
│   ├── icons/                       # 导航卡片所使用的 SVG 图标库
│   ├── images/                      # 界面背景图与默认占位图
│   └── fonts/                       # 自定义字体文件 (中英文)
├── config/                          # 全局配置目录
│   ├── site.json                    # 站点标题、描述、主题色等基础配置
│   ├── languages.json               # 多语言词条映射表
│   └── proxy.json                   # 开发环境下代理转发规则 (规避跨域)
├── data/                            # 核心数据目录
│   └── navigation.json              # 导航条目主数据 (分类、标题、URL、标签、权重)
├── docs/                            # 完整项目文档 (见文档导航章节)
│   ├── user-guide/
│   ├── developer/
│   └── ops/
├── scripts/                         # 工具脚本目录
│   ├── build.js                     # 生产环境构建主脚本 (生成静态 HTML)
│   ├── dev-server.js                # 本地开发服务器 (热重载)
│   ├── checker.js                   # 链接可用性巡检脚本 (基于 Puppeteer)
│   └── importer.js                  # 从 OPML / CSV 导入链接的转换脚本
├── src/                             # 前端源码目录
│   ├── components/                  # Vue / React 风格 UI 组件 (按功能拆分)
│   ├── layouts/                     # 页面布局模板 (首页、分类页、关于页)
│   ├── styles/                      # SCSS 样式文件 (含响应式断点)
│   └── utils/                       # 工具函数 (日期格式化、URL 解析、防抖)
├── tests/                           # 单元测试与集成测试目录
│   ├── unit/                        # Jest 单元测试用例
│   └── e2e/                         # Cypress 端到端测试脚本
├── .env.example                     # 环境变量范例 (用于配置巡检间隔、后端地址)
├── .gitignore                       # Git 忽略文件列表 (含 node_modules / dist)
├── CHANGELOG.md                     # 版本变更日志
├── CONTRIBUTING.md                  # 贡献者指南 (详细版)
├── LICENSE                          # MIT 许可证全文
├── README.md                        # 项目主说明文档 (即本文)
├── package.json                     # npm 项目清单 (含依赖与脚本命令)
└── tsconfig.json                    # TypeScript 编译配置 (若使用 TS 开发)
```

## 贡献指南

我们欢迎并感谢所有形式的贡献，包括但不限于新增导航节点、修复文档错误、提交功能改进或报告缺陷。请遵循以下步骤以确保协作流程顺畅：

1. **查阅现有议题与项目看板**：访问 GitHub Issues 和 Projects 页面，确认您想处理的工作尚未被其他人认领。若为新功能或新资源建议，请先开启一个议题并打上 `proposal` 标签，与维护者达成共识后再着手编码。

2. **分叉仓库并创建特性分支**：将主仓库 Fork 至个人账号，然后基于 `main` 分支创建新的分支，分支命名建议采用 `feature/描述` 或 `fix/描述` 的格式，例如 `feature/add-category-documentary`。

3. **编写或修改内容并运行自检**：若为导航数据变更，请严格按照 `data/navigation.json` 的 Schema 格式添加条目，并确保所有 URL 为可访问的公共资源。若为代码变更，请运行 `npm run test` 确保全部测试用例通过，并执行 `npm run lint` 检查代码风格一致性。

4. **提交符合规范的 Commit**：Commit 信息应遵循 Conventional Commits 规范，例如 `feat: add new category for documentary sites` 或 `fix: correct the broken link for zhongwenzimugaoqingw`。请保持 Commit 粒度适中，一个 Commit 只解决一个逻辑问题。

5. **发起 Pull Request 并等待审核**：将分支推送至个人 Fork 后，在主仓库发起 Pull Request。请在 PR 描述中清晰引用相关议题编号、列出变更摘要，并说明测试覆盖情况。维护者将在 48 小时内进行初审，必要时会请求补充修改。

## 常见问题

**问：MediaNav 是否提供视频播放或下载功能？为什么我点击某些链接后页面无法打开？**

答：MediaNav 严格定位于 URL 索引与导航管理，不涉及任何媒体内容的转码、存储或代理转发。点击链接后实际由您的浏览器直接请求目标域名，该域名的可用性完全由第三方服务方负责。若遇到无法打开的链接，请先确认您所处的网络环境能否访问该域名（例如部分站点可能限制海外 IP），同时您也可使用项目内置的巡检模块生成可用性报告。对于长期失效的链接，欢迎提交 Issue 或 Pull Request 予以移除或替换。

**问：我能否将 MediaNav 用于商业目的？是否需要保留版权声明？**

答：可以。本项目采用 MIT 许可证发布，您完全可以将代码用于个人、学术或商业用途，包括但不限于企业内部门户、SaaS 服务集成或二次销售。唯一的义务是在您的分发版本中保留原始的版权声明与许可证文本（通常位于 LICENSE 文件中）。我们鼓励但并非强制要求您在衍生作品中提及原始项目来源。

**问：导航数据更新频率是多少？我如何获取最新的外部资源变更？**

答：本项目作为一个开源协作项目，其数据更新完全依赖社区贡献者的提交节奏，官方不设定固定的强制刷新周期。但主仓库会不定期合并来自社区的改进，并在 Release 页面发布稳定版本。您可以通过以下两种方式保持同步：（1）定期执行 `git pull` 拉取最新 `main` 分支并重新构建；（2）在 GitHub 仓库中点击 “Watch” 按钮选择 “Releases only”，以便在新版本发布时接收邮件通知。若您需要更频繁的自动化更新，可自行编写定时任务定期拉取并构建。

## 许可证

MIT License。详见项目根目录下的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-25 22:00:27
