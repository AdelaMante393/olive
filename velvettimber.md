# NovaIndex 资源导航系统

NovaIndex 是一个面向技术社区与内容创作者的轻量化资源导航与外链聚合平台，旨在解决多源异构信息分散、检索效率低下、链接维护困难等问题。项目定位于个人开发者、小型团队及内容站点运营者，提供一套开箱即用的资源目录结构与标准化外链管理方案，帮助用户快速建立可扩展、可维护的在线资源索引体系。

NovaIndex 不依赖重型数据库后端，基于纯静态文件与前端路由实现资源分类展示与检索，适用于内网文档中心、开源项目推荐页、技术博客聚合站、影视资料索引库等多种场景。通过约定的目录结构与 Markdown 驱动的元数据配置，用户可以低成本完成资源录入、分类调整与版本回溯，同时保持对搜索引擎友好的语义化 HTML 输出。

## 功能概览

- **多级分类目录管理** 支持无限层级资源分类，每个分类可独立配置图标、描述与排序权重，满足复杂资源体系的组织需求。

- **标准化外链录入与校验** 提供统一的资源条目格式，支持标题、描述、标签、URL、状态码预期等字段，并内置链接可达性预检工具。

- **全文检索与即时过滤** 基于前端倒排索引实现标题与标签的模糊匹配，支持中英文混合检索，检索结果按相关性实时排序。

- **资源状态可视化标记** 自动标记外链的可用状态（有效、失效、待审核），支持手动标记维护过期链接，并生成维护报告。

- **自定义主题与布局** 内置明暗两套主题，允许通过 CSS 变量调整配色与间距，适配不同品牌视觉需求。

- **数据导入导出** 支持 JSON / YAML / CSV 三种格式的资源数据批量导入导出，便于与其他系统进行数据交换。

- **访问统计与热度排序** 基于 localStorage 记录资源点击频次，提供按热度、按更新时间、按创建时间三种排序维度。

## 应用场景

- **开源项目精选推荐页** 技术社区维护者可使用 NovaIndex 构建开源项目推荐目录，按编程语言、应用领域、成熟度等维度分类，方便社区成员快速发现优质项目。

- **企业内部知识库导航** 企业研发团队可将 NovaIndex 部署为内部文档导航系统，集中管理设计文档、API 文档、运维手册、测试报告等内部资源的访问入口。

- **影视或学习资料索引站** 内容创作者可利用 NovaIndex 构建影视资源或在线课程索引，分类整理不同语种、题材、清晰度的资源链接，供特定用户群体查阅。

- **个人书签管理与分享** 开发者可将个人长期积累的技术书签导出为 NovaIndex 格式，生成可公开访问的书签导航页，同步分享给团队或好友。

## 快速开始

执行以下命令完成项目的克隆、依赖安装与本地开发服务器启动：

```bash
git clone https://github.com/novaindex/novaindex.git
cd novaindex
npm install
npm run dev
```

启动成功后，访问控制台输出的本地地址（默认为 http://localhost:5173）即可查看导航站点。生产环境部署可使用 `npm run build` 生成静态文件，并将 `dist` 目录托管至任意 Web 服务器或对象存储服务。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | >= 18.17.0 | 运行时环境，用于构建工具与开发服务器 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库与提交变更 |
| 现代浏览器 | 最新两个主要版本 | 支持 ES Module 与 CSS Grid / Flexbox 布局 |
| 硬盘空间 | >= 200 MB | 包含源码、依赖包及构建产物 |
| 网络环境 | 可访问公网 | 用于首次安装依赖及外部资源链接访问 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 入门指南 | `/docs/guide/getting-started.md` | 如何快速部署第一个资源目录？如何新增第一条资源链接？ |
| 配置参考 | `/docs/config/categories.md` | 分类配置文件如何编写？排序和图标如何自定义？ |
| 维护手册 | `/docs/maintenance/link-check.md` | 如何批量检查外链有效性？如何处理失效链接？ |
| 开发进阶 | `/docs/development/theme-custom.md` | 如何创建自定义主题？如何修改检索逻辑？ |

## 资源列表

本节按类别整理项目收录的全部资源链接，所有链接均保持用户提供的原始格式，未做任何协议补全、域名改写或路径调整。

影视资源分类

- <code>zhongwenzaixiangaojinghaokan.org.cn</code>

- <code>rihanzhongwenzimu2.org.cn</code>

- <code>zhongwenzimubofang.org.cn</code>

- <code>mianfeikanjuwangzhan.org.cn</code>

- <code>renqizaixianguankan.org.cn</code>

- <code>zhongwenzimushipin.org.cn</code>

- <code>zaixianzumumianfeigaoqing.org.cn</code>

## 项目结构

项目采用模块化分层设计，源码目录与构建配置分离，便于维护与扩展。以下为项目核心目录及文件说明：

```
novaindex/
├── src/                                # 源代码主目录
│   ├── assets/                         # 静态资源文件
│   │   ├── icons/                      # 分类与功能图标（SVG 格式）
│   │   └── themes/                     # 主题样式变量文件（明暗两套）
│   ├── components/                     # 可复用 UI 组件
│   │   ├── ResourceCard/               # 资源卡片组件（含状态徽标）
│   │   ├── CategoryTree/               # 分类树导航组件（支持折叠展开）
│   │   └── SearchBar/                  # 搜索栏组件（含防抖与建议下拉）
│   ├── data/                           # 资源数据与分类配置
│   │   ├── categories.json             # 分类结构定义（层级、图标、顺序）
│   │   └── resources/                  # 按分类存放的资源条目文件（Markdown 格式）
│   ├── hooks/                          # 自定义 React Hooks
│   │   ├── useSearch.ts                # 全文检索逻辑封装
│   │   └── useLinkStatus.ts            # 外链状态检测与缓存逻辑
│   ├── pages/                          # 页面级组件
│   │   ├── HomePage/                   # 首页（分类概览与搜索入口）
│   │   └── CategoryPage/               # 分类详情页（资源列表与子分类）
│   ├── utils/                          # 工具函数库
│   │   ├── parser/                     # 资源条目解析器（支持 Markdown 与 YAML）
│   │   └── validator/                  # URL 格式校验与可达性检测工具
│   └── main.tsx                        # 应用入口文件
├── docs/                               # 项目文档（用户手册与开发指南）
│   ├── guide/                          # 入门指南文档
│   ├── config/                         # 配置参考文档
│   ├── maintenance/                    # 维护与运维手册
│   └── development/                    # 开发者进阶文档
├── scripts/                            # 辅助脚本（构建、数据迁移、链接检查）
│   ├── build.sh                        # 生产环境构建脚本
│   └── check-links.sh                  # 批量外链可达性检查脚本
├── public/                             # 公共静态资源（直接复制至构建输出）
│   └── favicon.ico                     # 站点图标
├── tests/                              # 单元测试与集成测试用例
│   ├── unit/                           # 组件与工具函数的单元测试
│   └── e2e/                            # 端到端场景测试（使用 Playwright）
├── .eslintrc.json                      # ESLint 代码规范配置
├── .prettierrc                         # Prettier 代码格式化配置
├── index.html                          # 主 HTML 模板文件
├── package.json                        # 项目依赖与脚本定义
├── tsconfig.json                       # TypeScript 编译配置
├── vite.config.ts                      # Vite 构建工具配置
└── README.md                           # 项目说明文档（当前文件）
```

## 贡献指南

NovaIndex 欢迎社区贡献，包括但不限于新增资源分类、优化检索算法、修复缺陷、补充文档等。请遵循以下流程提交贡献：

1. 查阅项目 Issue 列表，确认无重复议题后，在 Issue 中简述您计划贡献的内容，获得维护者反馈后再着手实现，以避免无效工作。

2. 从主仓库 Fork 项目至个人账户，克隆 Fork 后的仓库到本地，并配置 upstream 远程源以便同步主仓库更新。

3. 新建功能分支，分支命名采用 `feature/简述` 或 `fix/简述` 格式，在本地完成代码编写与自测，确保所有现有测试用例通过。

4. 提交代码时遵循 Conventional Commits 规范（如 `feat: 增加按标签筛选功能`），提交前运行 ESLint 与 Prettier 进行代码格式检查。

5. 向主仓库的 `main` 分支发起 Pull Request，在描述中关联相关 Issue 编号，并附上变更摘要与测试截图（如涉及 UI 改动）。维护者会在 3 个工作日内进行 Code Review。

## 常见问题

**Q：NovaIndex 是否支持动态后端数据库，例如 MySQL 或 PostgreSQL？**

A：NovaIndex 核心设计为纯静态方案，不强制依赖任何关系型数据库。所有资源数据以 JSON / Markdown 文件形式存储在 `src/data/` 目录下，构建时生成静态 HTML。若需要动态数据更新能力，建议配合 CI/CD 流水线在提交时自动触发构建，或自行扩展后端 API 读取同一数据源。项目团队暂不提供官方后端实现，但欢迎社区贡献适配层。

**Q：如何批量导入现有书签或外部 CSV 数据？**

A：项目内置了数据导入工具脚本，位于 `scripts/import.js`。您可以将 CSV 或 JSON 文件放置于 `scripts/input/` 目录下，运行 `npm run import` 并选择对应格式，工具会自动校验字段映射关系并生成符合 NovaIndex 规范的资源条目文件。对于浏览器导出的 HTML 书签文件，建议使用第三方转换工具先转为 JSON 格式后再导入。

**Q：外链状态检查工具是否会频繁请求外部域名，导致对方服务器压力？**

A：NovaIndex 内置的链接检查工具默认启用并发限制（最大并发数 5）和请求间隔（500ms），避免对目标服务器造成突发流量。同时工具仅发送 HEAD 请求获取响应状态码，不下载完整页面内容。检查结果会缓存 24 小时，减少重复检测。建议在非业务高峰期运行批量检查，并可通过配置文件调整并发参数。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-25 22:00:17
