# Navigator Arc

Navigator Arc 是一个面向数字内容研究、媒体存档与网络资源索引的专业技术导航工具。项目定位为技术驱动型的资源聚合与访问辅助系统，主要服务于内容分析人员、媒体研究者、数字档案管理员以及需要系统化访问特定网络资源的技术人员。本项目不提供任何内容存储、分发或代理服务，仅作为公开网络资源的可验证索引与访问链路集合，协助用户快速定位和访问目标信息源。

为解决网络资源分散、域名变更频繁、访问路径冗长等问题，Navigator Arc 提供结构化资源分类、链路健康检测、访问协议适配与基础访问统计功能，帮助用户在复杂网络环境中建立稳定、可维护的资源访问体系。

## 功能概览

**结构化资源索引**：按照内容类型、协议特征、域名层级等多维度对资源链接进行分类组织，支持快速筛选与定位。

**链路可达性检测**：定时检测收录域名的 DNS 解析状态与 HTTP/HTTPS 服务可用性，标记异常链路并生成状态报告。

**访问协议自适应**：自动识别并适配资源的实际协议类型，支持 HTTP 与 HTTPS 混合场景下的无缝访问切换。

**资源分类标签系统**：为每个资源条目分配可自定义的分类标签与备注字段，便于团队协作与个性化管理。

**静态资源快照记录**：记录每次资源访问的基础响应元数据，包括状态码、响应时间、内容类型等，用于链路质量评估。

**批量导入与导出**：支持标准格式的资源链接批量导入与导出，方便与其他系统进行数据交换与备份。

**访问统计看板**：提供按域名、协议、状态等维度的基础访问统计图表，帮助管理员了解整体资源生态状况。

## 应用场景

**数字媒体研究中的样本资源整理**：研究人员在进行网络媒体内容分析时，需长期跟踪特定来源的视频或文本资源。Navigator Arc 可帮助建立稳定的资源索引列表，记录每次访问的状态变化，辅助研究数据的可追溯性管理。

**内容分发网络的质量监测辅助**：运维人员或第三方监测机构可利用本项目的链路检测功能，定时检查分布于不同域名的资源服务状态，及时发现访问异常并触发告警流程。

**个人知识库的资源外链管理**：知识管理爱好者可使用本系统为个人笔记或文档库添加结构化的外链索引，配合标签与备注功能，有效避免链接失效导致的资料丢失问题。

**开源项目的外部依赖资源映射**：开发者在维护依赖外部数据或媒体资源的开源项目时，可通过 Navigator Arc 清晰记录所有外部资源的访问方式与状态，提高项目的可移植性与文档完整度。

## 快速开始

以下步骤指导您在本地环境中快速部署并运行 Navigator Arc 的基础服务。

```bash
# 1. 克隆项目仓库至本地
git clone https://github.com/navigator-arc/navigator-arc.git
cd navigator-arc

# 2. 安装项目依赖（使用 pip 与虚拟环境）
python -m venv venv
source venv/bin/activate  # Windows 系统请使用 venv\Scripts\activate
pip install -r requirements.txt

# 3. 初始化本地配置文件与资源索引数据库
cp config.example.yaml config.yaml
python scripts/init_db.py

# 4. 导入示例资源数据（包含本批次全部链接）
python scripts/import_resources.py --batch 21 --file data/batch_21.json

# 5. 启动本地开发服务器
python app.py --host 127.0.0.1 --port 8080
```

访问 `http://127.0.0.1:8080` 即可查看资源导航面板。如需执行链路检测，请运行 `python scripts/health_check.py` 并查看输出报告。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 及以上 | 核心运行环境，用于启动服务与执行脚本 |
| pip | 21.0 及以上 | Python 包管理工具，用于安装项目依赖 |
| SQLite | 3.35 及以上 | 本地轻量级数据库，用于存储资源索引与状态记录 |
| requests | 2.28.0 及以上 | HTTP 请求库，用于链路检测与访问协议适配 |
| PyYAML | 6.0 及以上 | YAML 配置文件解析，用于管理运行参数 |
| Flask | 2.2.0 及以上 | Web 服务框架，提供可视化导航面板 |
| pytest | 7.0 及以上 | 单元测试框架（仅开发环境必需） |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | docs/user_guide.md | 如何添加资源、查看统计、执行链路检测？ |
| 管理员指南 | docs/admin_guide.md | 如何配置检测频率、管理标签系统、备份数据？ |
| 开发参考 | docs/developer_api.md | 如何扩展资源导入器、自定义检测规则？ |
| 架构说明 | docs/architecture.md | 项目整体模块划分、数据流向与扩展点设计？ |

## 资源列表

本节列出本项目第 21/63 批次收录的全部网络资源链接。所有 URL 均按照用户提供的原始格式原样收录，未做任何协议补全、域名改写或路径调整。分类依据为域名主体语义特征，仅供参考。

媒体资源类别

<code>zaixianbofang2.org.cn</code>

<code>zhubofuli.org.cn</code>

视频字幕资源类别

<code>zhongwenzimudianying.org.cn</code>

<code>zhongwenzimuwangzhan.org.cn</code>

<code>zhongwenzimuzaixianmianfeikan2.org.cn</code>

在线播放与观看类别

<code>zaixianguankanwangyeshipin2.org.cn</code>

<code>zaixianzhongwenzimu.org.cn</code>

## 项目结构

```
navigator-arc/
├── app/                                # 主应用模块
│   ├── __init__.py                     # 应用工厂与初始化
│   ├── routes/                         # 路由视图层
│   │   ├── index.py                    # 首页导航与统计看板
│   │   ├── resources.py                # 资源增删改查接口
│   │   └── health.py                   # 链路检测结果展示
│   ├── models/                         # 数据模型层
│   │   ├── resource.py                 # 资源条目 ORM 模型
│   │   ├── check_record.py             # 检测记录模型
│   │   └── tag.py                      # 标签模型
│   └── templates/                      # 前端模板文件
│       ├── base.html                   # 基础布局模板
│       └── dashboard.html              # 统计看板页面
├── scripts/                            # 运维与辅助脚本
│   ├── init_db.py                      # 初始化数据库表结构
│   ├── import_resources.py             # 批量导入资源数据（支持批次参数）
│   ├── health_check.py                 # 执行全量链路检测并输出报告
│   └── export_resources.py             # 导出资源列表为 JSON/CSV
├── core/                               # 核心功能逻辑
│   ├── checker.py                      # 链路检测引擎（DNS + HTTP）
│   ├── parser.py                       # URL 解析与协议适配器
│   └── stats.py                        # 访问统计数据计算模块
├── data/                               # 数据存储目录
│   ├── batch_21.json                   # 第 21 批次原始资源数据
│   ├── batch_22.json                   # 第 22 批次（预留）
│   └── resource.db                     # SQLite 数据库文件（运行时生成）
├── tests/                              # 单元测试
│   ├── test_checker.py                 # 链路检测模块测试
│   ├── test_parser.py                  # URL 解析器测试
│   └── test_models.py                  # 数据模型操作测试
├── config.example.yaml                 # 配置文件示例（含检测间隔、端口等）
├── requirements.txt                    # 项目依赖清单
└── README.md                           # 项目说明文档（本文件）
```

## 贡献指南

欢迎对本项目进行贡献。请遵循以下步骤以确保代码质量和协作流程顺畅。

1. 复刻项目仓库至个人账户，并基于 `develop` 分支创建功能分支，分支命名格式为 `feature/功能简述` 或 `fix/问题简述`。

2. 编写或修改代码后，请确保所有现有单元测试通过，并为新增功能补充对应的测试用例，测试覆盖率不低于 80%。

3. 提交代码前运行代码格式化工具 `black` 与静态检查工具 `flake8`，保持代码风格与项目现有风格一致。

4. 发起合并请求至 `develop` 分支，并在请求描述中清晰说明变更目的、影响范围以及相关文档更新情况。

5. 项目维护者将在 5 个工作日内进行审查，如有修改意见将通过评论反馈，请及时响应。

## 常见问题

问：项目是否提供资源内容缓存或代理服务？

答：不提供。Navigator Arc 仅记录和展示资源链接的原始地址与访问状态，不存储、转发或镜像任何第三方内容。用户访问资源时，流量直接由用户端与目标服务器建立连接，项目本身不介入数据传输过程。

问：如何更新已收录资源的标签或分类信息？

答：可通过项目提供的 Web 管理界面或调用更新 API 接口进行修改。具体操作请参考 `docs/user_guide.md` 中的「资源管理」章节。对于批量更新，建议使用 `scripts/import_resources.py` 脚本配合 `--update` 参数重新导入数据。

问：链路检测功能是否会频繁访问目标服务器，造成对方负担？

答：检测模块默认采用单线程顺序执行，且每个目标域名的最小检测间隔为 6 小时，单次检测仅发送一次轻量级 HEAD 请求。用户可自行在配置文件中调整检测频率与超时时间，建议保持默认值以避免对第三方服务造成不必要的访问压力。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-25 21:59:38
