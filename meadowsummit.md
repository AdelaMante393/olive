# NexusIndex

NexusIndex 是一个面向中文数字内容消费者的轻量级资源导航与元信息聚合工具。项目定位为“可自托管的媒体与文本资源索引中间件”，主要服务于需要快速定位、分类管理与批量访问中文影视、字幕、音视频素材等外部资源的个人用户、内容研究者以及小型组织。NexusIndex 不存储任何实体文件，仅提供结构化链接管理、可用性探测与自定义标签系统，帮助用户从分散的书签与零散记录中解脱出来，构建可维护、可分享、可审计的外部资源访问入口。

## 功能概览

- **多源链接聚合管理**：支持手动添加、批量导入与分类存储外部资源链接，以扁平化命名空间与自定义标签双维度组织数据。
- **可用性状态扫描**：内置轻量级 HTTP 头探测与超时控制，定期标记链接响应状态码与响应时间，辅助用户识别失效或缓慢资源。
- **标签化分类体系**：允许为每条链接附加多个业务标签（如“字幕库”“高清源”“亚洲内容”），并支持标签组合筛选与布尔查询。
- **只读只写 API 接口**：提供基于 JSON 的 RESTful 管理接口，便于与其他自动化脚本或监控面板集成，支持链接增删改查与状态批量导出。
- **静态站点生成模式**：支持将当前索引数据一键导出为纯静态 HTML 目录页，无需数据库即可部署至任意 Web 服务器或对象存储。
- **访问频次统计与排序**：记录各链接被点击或通过 API 查询的次数，支持按热度与最近访问时间排序，辅助用户识别高频资源。
- **配置热加载与多环境支持**：通过环境变量与本地 YAML 配置文件实现开发、测试、生产环境隔离，修改配置后无需重启服务即可生效。

## 应用场景

- **个人媒体库书签替代**：用户可将分散在浏览器收藏夹中的数十个影视点播网站、字幕下载站与在线播放源统一录入 NexusIndex，并通过标签快速筛选出“高清 + 中文字幕”的组合，替代传统文件夹式书签管理。
- **内容研究团队的协作入口**：小型字幕组或内容分析团队可使用 NexusIndex 维护共享的外部参考链接池，每位成员均可补充新资源、标记失效链接，并通过静态导出功能生成团队内部导航页。
- **自动化监控与告警前置**：运维人员可将 NexusIndex 的可用性探测结果接入现有告警系统（如 Prometheus 或邮件通知），当关键资源链接连续三次不可达时触发告警，及早发现外部服务中断。
- **离线归档前的链接整理**：在批量下载或离线缓存前，用户通过 NexusIndex 导出按标签过滤后的链接清单，配合 wget 或 aria2 脚本实现批量拉取，减少人工复制粘贴的遗漏风险。

## 快速开始

以下指令适用于 Linux / macOS / Windows WSL 环境，要求已安装 Git、Node.js 18.x 及以上版本与 pnpm。

```bash
# 克隆仓库
git clone https://github.com/nexus-index/nexusindex.git
cd nexusindex

# 安装项目依赖
pnpm install

# 复制默认配置文件并修改监听端口与数据存储路径
cp .env.example .env

# 执行初始数据迁移与结构校验
pnpm run migrate

# 以开发模式启动服务，默认监听 3000 端口
pnpm run dev
```

启动成功后，访问 http://localhost:3000 即可进入管理界面。首次运行会自动生成示例链接数据，便于快速体验核心功能。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，推荐使用官方预编译二进制或 nvm 管理 |
| pnpm | 8.x 或 9.x | 包管理器，用于安装依赖与执行工作区脚本 |
| SQLite3 | 系统自带或由 better-sqlite3 编译 | 嵌入式数据库，无需额外安装，但编译依赖系统 build-essential |
| Git | 2.25 以上 | 用于克隆仓库与版本管理 |
| curl / wget | 任意稳定版本 | 用于可用性探测的外部调用，若缺失则回退至 Node.js 内置 http 模块 |
| 内存 | 最低 512 MB，推荐 1 GB | 运行时内存占用，静态导出时峰值可能上升至 1.5 GB |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | /docs/user-guide/overview.md | 如何添加链接、应用标签、执行探测与导出静态站点？ |
| 运维参考 | /docs/ops/deployment.md | 如何通过 systemd 或 Docker 部署生产环境？如何配置反向代理与 SSL？ |
| API 规范 | /docs/api/v1/endpoints.md | 管理接口的请求/响应格式、鉴权方式与分页参数是什么？ |
| 开发指南 | /docs/development/architecture.md | 项目模块划分、数据流向、测试策略与如何提交 PR？ |

## 资源列表

### 视频播放类

- <code>zhongwenzaixiangaojinghaokan.org.cn</code>
- <code>mianfeikanjuwangzhan.org.cn</code>
- <code>renqizaixianguankan.org.cn</code>
- <code>zaixianzumumianfeigaoqing.org.cn</code>

### 字幕与文本素材类

- <code>rihanzhongwenzimu2.org.cn</code>
- <code>zhongwenzimubofang.org.cn</code>
- <code>zhongwenzimushipin.org.cn</code>

## 项目结构

```
nexusindex/
├── apps/
│   ├── api/                         # RESTful 服务端核心模块
│   │   ├── src/
│   │   │   ├── controllers/         # 链接与标签的 CRUD 控制器
│   │   │   ├── services/            # 探测、统计与导出业务逻辑
│   │   │   └── routes/              # 路由定义与中间件注册
│   │   └── tests/                   # 单元与集成测试用例
│   └── web/                         # 管理后台前端界面（React + Tailwind）
│       ├── src/
│       │   ├── pages/               # 列表、详情、配置与导出页面
│       │   ├── components/          # 可复用 UI 组件（表格、标签选择器）
│       │   └── hooks/               # 自定义数据请求与状态管理钩子
├── packages/
│   ├── core/                        # 数据模型、验证器与常量定义
│   │   └── schemas/                 # Zod 与 JSON Schema 定义
│   ├── scanner/                     # 独立可用性探测模块，支持并发与超时重试
│   │   └── strategies/              # 不同协议（http/https）的探测策略
│   └── exporter/                    # 静态站点生成器，支持多种模板引擎
│       └── templates/               # 默认 HTML 模板与样式文件
├── configs/
│   ├── default.yaml                 # 默认端口、日志级别与探测间隔
│   └── production.yaml              # 生产环境覆盖配置（关闭调试、启用压缩）
├── data/                            # SQLite 数据库文件与迁移脚本存放目录（运行时生成）
├── docs/                            # 完整文档源码，使用 Markdown 与 Mermaid 绘图
├── scripts/
│   ├── migrate.js                   # 数据库迁移与回滚脚本
│   └── seed.js                      # 示例数据填充脚本
├── .env.example                     # 环境变量模板（JWT 密钥、数据库路径）
├── docker-compose.yml               # 开发与测试环境容器编排
├── Dockerfile                       # 多阶段构建镜像文件
├── package.json                     # 根工作区依赖与脚本定义
└── README.md                        # 项目入口说明文档（本文档）
```

## 贡献指南

1. **查阅现有议题与看板**：访问 GitHub Issues 与 Projects 看板，确认当前待解决的问题或计划中的功能，避免重复工作。对于新功能或较大重构，建议先创建议题并说明动机与方案，获得维护者反馈后再着手开发。
2. **派生仓库并创建特性分支**：将本仓库派生至个人账号下，基于 main 分支创建命名为 `feat/描述` 或 `fix/描述` 的分支，遵循语义化命名。分支应保持原子性提交，每次提交附带清晰的中文或英文摘要。
3. **编写或更新测试用例**：所有新增 API 端点或修改核心探测逻辑均需提供对应的单元测试与集成测试，确保测试覆盖率不低于当前基准。执行 `pnpm run test` 验证全部用例通过后，再发起拉取请求。
4. **同步文档与示例**：若修改了配置结构、API 响应字段或命令行参数，必须同步更新 `/docs` 下对应章节以及 `.env.example` 文件。静态导出模板的样式变更需附带前后对比截图。
5. **发起拉取请求并参与审查**：推送分支后，在 GitHub 上创建拉取请求，填写 PR 模板中的检查清单，关联相关议题编号。审查过程中及时响应评论并推送修正提交，合入前维护者将进行最终功能验收。

## 常见问题

**问：可用性探测是否会误伤目标站点？探测频率如何控制？**
答：探测模块默认使用 HEAD 请求，仅获取响应头，不下载实体内容，单次超时设置为 5 秒。系统默认每 6 小时对所有链接执行一次全量探测，该间隔可通过配置文件的 `scanner.interval` 字段调整至 1 至 24 小时。对于明确返回 429 或 503 状态码的站点，系统会自动进入指数退避冷却期，冷却期内不再发起请求。

**问：如何从旧版书签或 CSV 文件迁移数据？**
答：项目根目录下提供了 `scripts/import-csv.js` 辅助脚本，接受以逗号或制表符分隔的 CSV 文件，要求包含 `url`、`title` 与可选的 `tags` 列。执行 `pnpm run import -- --file ./bookmarks.csv` 即可将数据导入当前数据库。若需从浏览器导出 HTML 书签，可先使用在线工具转换为 CSV 格式再执行导入。

**问：静态导出生成的 HTML 页面是否包含所有链接？能否自定义主题？**
答：默认导出会包含当前数据库内全部链接及其标签与最近探测状态，按字母顺序或自定义排序规则排列。您可以在 `packages/exporter/templates/` 目录下覆盖默认的 Handlebars 模板与 CSS 文件，或通过 `--template` 参数指定外部模板目录，满足品牌化或内网部署需求。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-25 21:59:38
